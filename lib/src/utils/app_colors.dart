import 'package:flutter/material.dart';

// This class holds all the color constants for the app
class AppColors {
  // Prevent instantiation
  AppColors._();

  static const Color primary = Color(0xFF0d86cb); // Deep Sky Blue
  static const Color secondary = Color(0xFFfbfefd); // White
  static const Color accent = Color(0xFF1E88E5); // Medium Blue
  static const Color text = Color(0xFF424242); // Greyish text
  static const Color background = Color(0xFFfbfefd); // Light background
}
