import 'package:flutter/material.dart';

// This class holds all the color constants for the app
class AppColors {
  // Prevent instantiation
  AppColors._();

  static const Color primary = Color(0xFF0D86CB);
  static const Color secondary = Color(0xFFFBFEFD);
}
